server:
  port: 8881

spring:
  application:
    name: netty_server

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8880/eureka/

  management:
    endpoints:
      web:
        exposure:
          include: "*"
        cors:
          allowed-origins: "*"
          allowed-methods: "*"